*filter
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
:OUTPUT DROP [0:0]
# Loopback
-A INPUT -i lo -j ACCEPT
-A OUTPUT -o lo -j ACCEPT
# Established connections
-A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
-A OUTPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
# DHCP client (lease acquisition and renewal)
-A OUTPUT -p udp --sport 68 --dport 67 -j ACCEPT
-A INPUT -p udp --sport 67 --dport 68 -j ACCEPT
# Inbound: HTTP/HTTPS only
-A INPUT -p tcp --dport 80 -j ACCEPT
-A INPUT -p tcp --dport 443 -j ACCEPT
# Inbound: RADIUS (network device authentication)
-A INPUT -p udp --dport 1812 -j ACCEPT
-A INPUT -p icmp --icmp-type echo-request -j ACCEPT
# Outbound: DNS (for Docker pulls, license checks, firmware fetching)
-A OUTPUT -p udp --dport 53 -j ACCEPT
-A OUTPUT -p tcp --dport 53 -j ACCEPT
# Outbound: HTTPS (GHCR, license server, firmware API, Slack webhooks)
-A OUTPUT -p tcp --dport 443 -j ACCEPT
# Outbound: HTTP (device management — APs, switches, CPEs on local network)
-A OUTPUT -p tcp --dport 80 -j ACCEPT
# Outbound: NTP (time sync)
-A OUTPUT -p udp --dport 123 -j ACCEPT
# Outbound: SSH (git backup via SSH)
-A OUTPUT -p tcp --dport 22 -j ACCEPT
# Outbound: LDAP/LDAPS (RADIUS LDAP proxy authentication)
-A OUTPUT -p tcp --dport 389 -j ACCEPT
-A OUTPUT -p tcp --dport 636 -j ACCEPT
# Outbound: ICMP (ping for connectivity checks)
-A OUTPUT -p icmp --icmp-type echo-request -j ACCEPT
# Docker: allow established/related forwarded traffic (survives iptables restore)
-A FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
# Docker inter-container communication (bridge and compose networks)
-A FORWARD -i docker0 -j ACCEPT
-A FORWARD -o docker0 -j ACCEPT
-A FORWARD -i br+ -j ACCEPT
-A FORWARD -o br+ -j ACCEPT
COMMIT
